<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.bin.soundcloud">
    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"/>
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"/>
    <uses-permission android:name="android.permission.READ_PHONE_STATE" />
    <permission android:name="android.permission.MEDIA_CONTENT_CONTROL" />

    <application
        android:largeHeap="true"
        android:allowBackup="true"
        android:icon="@mipmap/ic_soundcloud"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_soundcloud"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".first_activity"
            >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
        <activity android:name=".item_song" />
        <activity android:name=".listsong_vertical" />
        <activity android:name=".MainActivity" />
        <activity android:name=".home" />
        <activity android:name=".forgot_layout" />
        <activity android:name=".create_by_email" />
        <activity android:name=".create_account" />
        <activity android:name=".after_create" />
        <activity android:name=".signIn" />

    </application>

</manifest>